# Create keypair for acra-connector only
acra-connector: false

# Create keypair for acraserver only
acraserver: false

# Create symmetric key for acra_configui's basic auth db
basicauth: false

# Client id
client_id: client

# path to config
config: 

# dump config
dumpconfig: false

# Generate new random master key and save to file
master_key: 

# Folder where will be saved keys
output: .acrakeys

# Folder where will be saved public key
output_public: .acrakeys

# Create keypair for data encryption/decryption
storage: false

