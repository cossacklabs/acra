version: '3'

services:
  mydb:
    image: postgres:9.6
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_PORT: 5432
      PGDATA: /pgdata
    ports: ["5432:5432"]
    volumes: ["./pgdata:/pgdata"]