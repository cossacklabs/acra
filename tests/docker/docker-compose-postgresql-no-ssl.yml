version: '3'

services:
  mydb:
    image: postgres:11
    environment:
      POSTGRES_DB: test
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
      POSTGRES_PORT: 5432
      PGDATA: /pgdata
    ports: ["5432:5432"]
