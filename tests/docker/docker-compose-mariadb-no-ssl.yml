version: '3'

services:
  mydb:
    image: mariadb:10.7
    environment:
      MYSQL_DATABASE: test
      MYSQL_USER: test
      MYSQL_PASSWORD: test
      MYSQL_ROOT_PASSWORD: root
    ports: [ "3306:3306" ]
